---
import type { Product } from "../../netlify/functions/get-product-list";

const response = await fetch(`https://precious-manatee-ee242e.netlify.app/.netlify/functions/get-product-list`);
const body = await response.json();
const products: Product[] = body.data;
---
<ul role="list" class="grid grid-cols-1 gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
  {products.map((product) => {
    return (
      <div class="group relative">
        <div class="min-h-80 aspect-w-1 aspect-h-1 w-full overflow-hidden rounded-md bg-gray-200 group-hover:opacity-75 lg:aspect-none lg:h-80">
          <img src='https://trendygolf.imgix.net/uploads/asset_file/asset_file/391818/1666020365.2249591-1327037-496-01_gfseqp.jpg?auto=compress%2Cformat&bg=fff&crop=faces&cs=tinysrgb&dpr=1.5&fit=crop&fm=pjpg&h=564&q=80&w=564' alt="Front of men&#039;s Basic Tee in black." class="h-full w-full object-cover object-center lg:h-full lg:w-full"/>
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <span aria-hidden="true" class="absolute inset-0"></span>
              {product.title}
            </h3>
          </div>
          <p class="text-sm font-medium text-gray-900">£19.99</p>
        </div>
      </div>
    )
  })}
</ul>
